#include "main.h"

/**
* add - function that adds two integers and returns the result.
*
* @a: first num
* @b: second num
*
* Return: a + b
*
*/

int add(int a, int b)
{
	int add;

	add = a + b;

	return (add);
}
