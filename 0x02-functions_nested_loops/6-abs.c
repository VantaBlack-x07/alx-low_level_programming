#include "main.h"

/**
* _abs - function that computes the absolute value of an integer.
*
* @n: number used to compute
*
* Return: n
*
*/

int _abs(int n)
{
	if (n < 0)
{
		n *= -1;
		return (n);
}
}
