#include "main.h"

/**
* _abs - function that computes the absolute value of an integer.
*
* @int: number used to compute
*
* Return: int
*
*/
int _abs(int)
{
	if (int < 0)
{
		return (-int);
}
	else if (int > 0)
		return (int);
}
