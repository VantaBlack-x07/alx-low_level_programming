#include "main.h"

/**
* _abs - function that computes the absolute value of an integer.
*
* @int: number used to compute
*
* Return: int
*
*/
int _abs(int n)
{
	if (n < 0)
{
		return (-n);
}
	else if (n > 0)
{
		return (n);
}
}
