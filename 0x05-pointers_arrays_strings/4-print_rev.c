#include "main.h"

/**
* print_rev - function that prints a string, in reverse.
*
* @s: is a parameter
*/

void print_rev(char *s)

{
	for (i = 0; s[i] = '\0'; i++)
	{
		if (s = '0')
		{
			s--;
			_putchar(s);
		}
	}
	_putchar('\n');
}
