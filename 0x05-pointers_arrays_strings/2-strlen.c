#include "main.h"

/**
* _strlen - function that returns the length of a string.
*
* @s: is a parameter.
*
* Return: int the length.
*/

int _strlen(char *s)

{
	int i = 0, c = 0;

	for (; i != '\0'; i++)
	{
		if (s[i] != '\0')
		{
			c++
		}
	}


return (c);

}
